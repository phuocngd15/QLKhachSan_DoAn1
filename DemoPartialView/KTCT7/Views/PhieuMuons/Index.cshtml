@model IEnumerable<KTCT7.Models.PhieuMuon>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NgayMuon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayTra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaDocGia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NgayMuon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayTra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaDocGia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SoPhieu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SoPhieu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SoPhieu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
