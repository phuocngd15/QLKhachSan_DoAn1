@model IEnumerable<HappyHoTel.Models.PhieuDatPhong>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TinhTrang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayNhanDuTinh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayTraDuTinh)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TinhTrang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayNhanDuTinh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayTraDuTinh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PhieuDatPhongId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PhieuDatPhongId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PhieuDatPhongId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
